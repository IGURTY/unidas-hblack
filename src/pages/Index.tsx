// Update this page (the content is just a fallback if you fail to update the page)

import CarOffersDashboard from "@/components/CarOffersDashboard";

const Index = () => {
  return <CarOffersDashboard />;
};

export default Index;
